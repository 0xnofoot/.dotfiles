# lazygit 最小配置 — 仅接入 delta 作为 pager，其余保持默认。
# macOS/Linux 统一读取 ~/.config/lazygit/config.yml（依赖 zsh/env.zsh 导出 XDG_CONFIG_HOME）

git:
  pagers:
    - colorArg: always
      pager: delta --paging=never --side-by-side
